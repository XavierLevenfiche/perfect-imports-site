# Perfect Imports — website

Single-page site for perfect-imports.com. Edit `index.html` (all CSS is inline in `<style>`),
commit, push → GitHub Pages redeploys live in ~30s.

- `index.html` — the page (edit here)
- `assets/` — logo + brand images
- `CNAME` — custom domain (perfect-imports.com)
- `DEPLOY.md` — one-time deploy + DNS steps

Built 2026-07-01. PerfectTed-styled. Honest positioning — no over-claimed clients.
